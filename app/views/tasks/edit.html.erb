<h1>Edit task</h1>

<%= form_for(@task) do |f| %>
  <%= f.label :title %>
  <%= f.text_field :title %>
  <%= f.label :details %>
  <%= f.text_field :details %>
  <%= f.check_box :completed %>
  <%= f.submit %>
<% end %>

<br>
<%= @task.completed %>
<br>
<br>
<br>


<%# <% check_box(@task, completed, checked_value = true, unchecked_value = "0") %>

<br>
<br>
<%= link_to "Delete #{@task.title}",
            task_path(@task),
            method: :delete,
            data: { confirm: "Are you sure?" } %>
<br>
<br>
<%= link_to 'Back to list', tasks_path %>